# Release Candidate 2026-05-19 RC1

Base:

- Branch: `main`
- Source PR: `https://github.com/neonwatty/codex-terminal-manager/pull/76`
- Merged QA PR commit: `8b3bc07`
- Verification commit: `d739980`
- Command artifacts: `docs/release-candidates/2026-05-19-rc1-artifacts/`

Required gates:

- [x] `python3 -m unittest discover -s tests -v`
- [x] `scripts/check-resource-warnings`
- [x] `python3 -m py_compile scripts/workerctl scripts/check-resource-warnings workerctl/*.py`
- [x] `bash -n scripts/live-smoke`
- [x] `bash -n scripts/live-smoke-repeat`
- [x] `scripts/live-smoke-repeat 3`
- [x] `scripts/workerctl sessions --state active`
- [x] `scripts/workerctl reconcile --stale-cycles-seconds 1`

Results:

- Unit tests: `01-unittest.*` passed 352 tests in 12.420s.
- ResourceWarning gate: `02-resource-warnings.*` passed 352 tests in 11.953s with no ResourceWarning failure.
- Compile: `03-py-compile.*` passed.
- Shell syntax: `04-live-smoke-syntax.*` and `05-live-smoke-repeat-syntax.*` passed.
- Repeat live smoke artifact: `docs/live-qa-artifacts/2026-05-19-live-smoke-repeat-repeat-20260519081324/`.
- Active sessions: `06-sessions-active.*` captured `[]`.
- Reconcile: `07-reconcile.*` captured no dangling bindings, dead PID sessions, or stuck tasks; schema health ok.

Decision:

- RC accepted: yes
- Notes: `summary.jsonl` in the repeat artifact records runs 1-3 with `status: 0` and `reconcile_clean: true`. The verification commit includes the RC wrapper added after the merged QA PR, so this receipt records both the merged PR commit and the exact post-merge verification commit.
